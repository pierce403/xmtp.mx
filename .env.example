## Frontend
# Optional: xmtp.mx Reown/WalletConnect project ID. WalletConnect is omitted
# when unset; project IDs from another app/origin must not be reused.
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Optional: used only for a legacy GitHub Pages project site (e.g. "/xmtp.mx").
# Leave empty for Cloudflare Workers Static Assets and the xmtp.mx custom domain.
NEXT_PUBLIC_BASE_PATH=

# Optional: improves ENS resolution reliability in the browser
NEXT_PUBLIC_MAINNET_RPC_URL=

# Optional: XMTP environment for the browser client (local|dev|production). Defaults to production.
NEXT_PUBLIC_XMTP_ENV=production

# Public production inbox for the XMTP -> email relay (64 hex characters, no private key).
NEXT_PUBLIC_XMTP_RELAY_INBOX_ID=

## SMTP -> XMTP bridge (not deployed on GitHub Pages)
# Wallet used by the bridge to publish XMTP messages (keep this secret)
XMTP_BOT_PRIVATE_KEY=

# Optional: XMTP environment (local|dev|production). Defaults to production.
XMTP_ENV=production

# Optional: improves ENS resolution reliability on the server
ETHEREUM_RPC_URL=
